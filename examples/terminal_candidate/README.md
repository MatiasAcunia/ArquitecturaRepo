# Terminal Candidate Synthetic Example

This directory demonstrates a campaign candidate that has reached a strategic return boundary.

Everything is synthetic.

The relationship under test is:

`Product State -> Charter -> Terminal -> Independent Review -> Bootstrap`

The campaign is no longer actively mutating the candidate. Execution authority is superseded while Planner acceptance remains open.
