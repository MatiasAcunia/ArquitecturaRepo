# Active Campaign Synthetic Example

This directory demonstrates a coherent active engineering campaign.

Everything is synthetic.

The example exists to exercise cross-file validation:

`Product State -> Charter -> Identity -> Execution Authority -> Checkpoint -> Bootstrap`

No file represents a real project, person, provider, repository, branch or campaign.
